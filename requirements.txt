# Orchestration & Database
supabase
pydantic
python-dotenv==1.0.0

# LLM / AI
anthropic

# Geometry & CAD Processing
ezdxf==1.1.4

# 3D Processing
trimesh==4.0.8
numpy
# pyrender requires OpenGL context, which can be tricky headlessly, 
# but we add it for the placeholder. We might use headless Blender instead if this fails.
pyrender
Pillow

# Optional: web framework if we want to run orchestrator as a server receiving webhooks from n8n
fastapi==0.104.1
uvicorn==0.24.0
python-multipart
anyio<4.0.0
PyPDF2
requests
